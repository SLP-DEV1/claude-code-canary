# Project instructions

- First inspect the failing behavior and identify the narrowest root cause.
- Make the smallest change that fixes the task.
- Run the most relevant focused tests first, then the broader verification command.
- Do not change public APIs unless the task explicitly requires it.
- Avoid unrelated refactors, formatting churn, or dependency changes.
